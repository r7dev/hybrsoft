<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Hybrsoft.EnterpriseManager.Controls.ModernTitle"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Hybrsoft.EnterpriseManager.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid x:Name="AppTitleBar" Height="48">
		<Grid.ColumnDefinitions>
			<ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
			<ColumnDefinition x:Name="IconColumn" Width="Auto"/>
			<ColumnDefinition x:Name="TitleColumn" Width="Auto"/>
			<ColumnDefinition x:Name="LeftDragColumn" Width="*"/>
			<ColumnDefinition x:Name="SearchColumn" Width="4*" MinWidth="220"/>
			<ColumnDefinition x:Name="RightDragColumn" Width="*" MinWidth="48"/>
			<ColumnDefinition x:Name="AccountColumn" Width="Auto"/>
			<ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
		</Grid.ColumnDefinitions>
		<Image x:Name="TitleBarIcon"
			   Source="ms-appx:///Assets/default.ico"
			   Grid.Column="1"
			   Width="16" Height="16"
			   Margin="8,0,4,0"/>
		<TextBlock x:Name="TitleBarTextBlock"
				   Text="{x:Bind Title, Mode=OneWay}"
				   Style="{StaticResource CaptionTextBlockStyle}"
				   Grid.Column="2"
				   VerticalAlignment="Center">
		</TextBlock>
		<Grid x:Name="PersonPic"
			  Grid.Column="6"
			  Visibility="{x:Bind IsUserInfoHidden, Converter={StaticResource InverseBoolToVisibilityConverter}}">
			<PersonPicture DisplayName="{x:Bind DisplayName}"
						   Height="32" Margin="0,0,16,0"
						   ProfilePicture="{x:Bind PictureSource, Converter={StaticResource ObjectToImageConverter}}"/>
			<Button BorderThickness="0"
					HorizontalAlignment="Stretch"
					VerticalAlignment="Stretch"
					Opacity="0">
				<Button.Flyout>
					<Flyout>
						<StackPanel Orientation="Horizontal">
							<PersonPicture DisplayName="{x:Bind DisplayName}"
										   Height="48" Margin="0,0,16,0"
										   ProfilePicture="{x:Bind PictureSource, Converter={StaticResource ObjectToImageConverter}}"/>
							<StackPanel>
								<TextBlock Text="{x:Bind DisplayName}" FontWeight="Bold"/>
								<TextBlock Text="{x:Bind AccountName}" FontSize="12"/>
							</StackPanel>
							<HyperlinkButton Click="OnLogoff" Content="Logoff" Margin="16,0,0,0"/>
						</StackPanel>
					</Flyout>
				</Button.Flyout>
			</Button>
		</Grid>
	</Grid>
</UserControl>
