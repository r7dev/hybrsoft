<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Hybrsoft.EnterpriseManager.Controls.ModernTitle"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Hybrsoft.EnterpriseManager.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<TitleBar Title="{x:Bind Title, Mode=OneWay}"
			  Subtitle="Preview"
			  BackRequested="TitleBar_BackRequested"
			  IsBackButtonEnabled="{x:Bind IsBackButtonEnabled, Mode=TwoWay}"
			  IsBackButtonVisible="{x:Bind IsBackButtonVisible, Mode=OneWay}"
			  IsPaneToggleButtonVisible="{x:Bind IsPaneToggleButtonVisible, Mode=OneWay}"
			  PaneToggleRequested="TitleBar_PaneToggleRequested">
		<TitleBar.IconSource>
			<ImageIconSource ImageSource="ms-appx:///Assets/default.ico" />
		</TitleBar.IconSource>
		<TitleBar.Content>
			<AutoSuggestBox Width="360"
							VerticalAlignment="Center"
							PlaceholderText="Search.."
							QueryIcon="Find"
							Visibility="{x:Bind IsSearchBoxVisible, Converter={StaticResource DefaultBoolToVisibilityConverter}}"/>
		</TitleBar.Content>
		<TitleBar.RightHeader>
			<Grid x:Name="PersonPic"
				  Grid.Column="6"
				  Visibility="{x:Bind IsUserInfoVisible, Converter={StaticResource DefaultBoolToVisibilityConverter}, Mode=OneWay}">
				<PersonPicture DisplayName="{x:Bind DisplayName}"
							   Height="32" Margin="0,0,16,0"
							   ProfilePicture="{x:Bind PictureSource, Converter={StaticResource ObjectToImageConverter}}"/>
				<Button BorderThickness="0"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						Opacity="0">
					<Button.Flyout>
						<Flyout>
							<StackPanel Orientation="Horizontal">
								<PersonPicture DisplayName="{x:Bind DisplayName}"
											   Height="48" Margin="0,0,16,0"
											   ProfilePicture="{x:Bind PictureSource, Converter={StaticResource ObjectToImageConverter}}"/>
								<StackPanel>
									<TextBlock Text="{x:Bind DisplayName}" FontWeight="Bold"/>
									<TextBlock Text="{x:Bind AccountName}" FontSize="12"/>
								</StackPanel>
								<HyperlinkButton Click="OnLogoff" Content="Logoff" Margin="16,0,0,0"/>
							</StackPanel>
						</Flyout>
					</Button.Flyout>
				</Button>
			</Grid>
		</TitleBar.RightHeader>
	</TitleBar>
</UserControl>
